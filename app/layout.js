import './globals.css';

export const metadata = {
  title: 'ClothVex | Unstitched Men & women Fabrics',
  description: 'Shop premium unstitched for men and women clothing at ClothVex. Order directly via WhatsApp.'
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
